const Loading = () => {
  return (
    <div className="w-screen h-screen flex items-center justify-center">
      <p className="text-9xl font-bold text-yellow-500">Loading...</p>
    </div>
  );
};

export default Loading;
